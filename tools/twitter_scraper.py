import tweepy #we'll make use of tweepy, a wrapper around the Twitter API: http://tweepy.readthedocs.io

def getTweetsFrom(username):
    """Simple function that given a twitter username, obtains some of his/her tweets.
    Could take an additional number_of_tweets argument, or tweets_since argument,
    that would mark the end of the tweets we want to get.
    The function should return just the tweet's text content."""
    pass

